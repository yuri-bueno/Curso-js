const pontuação = 100

const nivel = pontuação >= 70 ? "arroz bom" : "arroz merda";

//variavel = (condição) ? (verdade)  : (falso);

const sum = (a, b) => a + b

// variavel = (x,y)=> x+b

const corUsuario = null;
const corPadrão = corUsuario || "preto";
//se a primeira opção for falsy vai na segunda


// variavel = (x,y)=> x+b
//if (condição) return qualquercoisa;
//const nivel = pontuação >= 70 ? "arroz bom" : "arroz merda";